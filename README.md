# Test Unpinned Actions

This repository contains GitHub Actions with unpinned versions for Build-Guard testing.

**Expected Policy Failures:**
- POL-SEC-07: Unpinned GitHub Actions (using tags instead of SHA)
